# Settings and assistant exploratory QA journeys

1. Launch fresh isolated first run; type minimal introduction; try continue/back and inspect favorites requirement; complete three choices and appearance; inspect furnished welcome.
2. Open Settings via Cmd+,; inspect General input validation, all root/nested settings, theme updates, narrow-window presentation and missing search/discoverability.
3. Edit personal memory; verify save/version/revert, add location and freeform fact, forget and restore, search/history, and draft navigation behavior.
4. Change shortcut, attempt conflicting and reserved shortcuts, reset a default after its binding is reused; verify actual outcome and persistence.
5. Open Reminders from Settings and keyboard; inspect empty schedule and create affordances. Only local fixture reminder setup if required for populated states.
6. In isolated deterministic demo: submit ambiguous prompt, answer clarification, reject approval, restart/reopen/delete thread, examine takeover/return and evidence when feasible.

Each significant state gets a native Electron screenshot and log. No live sign-in, personal-data import, external messages or purchases. E2E profile and deterministic agent disclosed.
