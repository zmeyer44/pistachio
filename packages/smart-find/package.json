{
  "name": "@pistachio/smart-find",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Find in page by meaning: the page scripts, the Jev ranking, the match policy and the host-agnostic session (docs/smart-find.md).",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src test --max-warnings 0"
  },
  "dependencies": {
    "@pistachio/shell-contracts": "workspace:*",
    "ai": "^7.0.107"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22.15.0",
    "eslint": "^9.39.1",
    "typescript": "5.9.2",
    "vitest": "^3.2.0"
  }
}
